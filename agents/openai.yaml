interface:
  display_name: "Taste + GSAP Frontend"
  short_description: "Build purposeful, accessible GSAP motion"
  default_prompt: "Use $taste-gsap-frontend to design and implement a motion-rich marketing frontend. Preserve the existing stack, state the motion level, and verify reduced-motion and mobile fallbacks."
